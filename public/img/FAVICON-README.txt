Tab icon (favicon) — 避免标签页图标被压扁

请在此目录放置一张「正方形」图片作为标签页图标：

  文件名：favicon.png
  建议尺寸：32×32 或 64×64 像素（正方形）

若使用横向 logo（如 xiuzi_logo.png）作为 favicon，浏览器会把它压成小方块，导致变形。
请使用 logo 的正方形版本（例如只保留圆形/月牙部分并居中放在正方画布上），
导出为 favicon.png 放到 public/img/ 即可。
